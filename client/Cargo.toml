[package]
name = "client"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.16.0", features = ["dynamic_linking"] }
bevy_spacetimedb = "0.5.0"


[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
